diff --git a/android/lib/src/main/java/org/libreoffice/androidlib/LOActivity.java b/android/lib/src/main/java/org/libreoffice/androidlib/LOActivity.java
index 46c5a85..d5f9cd4 100644
--- a/android/lib/src/main/java/org/libreoffice/androidlib/LOActivity.java
+++ b/android/lib/src/main/java/org/libreoffice/androidlib/LOActivity.java
@@ -74,6 +74,7 @@ import java.nio.channels.ReadableByteChannel;
 import java.nio.charset.Charset;
 import java.util.Base64;
 import java.util.HashMap;
+import java.util.Locale;
 import java.util.Map;
 import java.util.Objects;
 import java.util.concurrent.LinkedBlockingQueue;
@@ -385,6 +386,10 @@ public class LOActivity extends AppCompatActivity {
         // only set it to false if it returns false otherwise it can break previous controls
         if (!canDocumentBeExported())
             isDocEditable = false;
+        if (isPdfDocument()) {
+            isDocEditable = false;
+            Log.d(TAG, "Disabled editing: KOffice opens PDF documents in view-only mode");
+        }
         if (mTempFile != null)
         {
             mWebView = (COWebView) findViewById(R.id.browser);
@@ -1260,6 +1265,20 @@ public class LOActivity extends AppCompatActivity {
         return cR.getType(data);
     }
 
+    private boolean isPdfDocument() {
+        String mimeType = getIntent().getType();
+        if (mimeType == null)
+            mimeType = getMimeType();
+        if ("application/pdf".equalsIgnoreCase(mimeType))
+            return true;
+
+        String fileName = getFileName(true);
+        Uri data = getIntent().getData();
+        if (fileName == null && data != null)
+            fileName = data.getLastPathSegment();
+        return fileName != null && fileName.toLowerCase(Locale.ROOT).endsWith(".pdf");
+    }
+
     private String getFileName(boolean withExtension) {
         Cursor cursor = null;
         String filename = null;
@@ -1276,6 +1295,9 @@ public class LOActivity extends AppCompatActivity {
     }
 
     private void requestForCopy() {
+        if (isPdfDocument())
+            return;
+
         final boolean canBeExported = canDocumentBeExported();
         buildPrompt(getString(R.string.ask_for_copy), "", canBeExported ? getString(R.string.edit_copy) : getString(R.string.use_odf), getString(R.string.view_only), new DialogInterface.OnClickListener() {
             @Override
diff --git a/browser/src/canvas/sections/CommentSection.ts b/browser/src/canvas/sections/CommentSection.ts
index a05d680..53b72db 100644
--- a/browser/src/canvas/sections/CommentSection.ts
+++ b/browser/src/canvas/sections/CommentSection.ts
@@ -1155,11 +1155,11 @@ export class Comment extends CanvasSectionObject {
 	}
 
 	public canRemove(): boolean {
-		return this.isAuthor() || !this.map.isReadOnlyMode();
+		return !app.isPdfDocument() && (this.isAuthor() || !this.map.isReadOnlyMode());
 	}
 
 	public canModerate(): boolean {
-		return this.isAuthor() || app.isCommentEditingAllowed();
+		return !app.isPdfDocument() && (this.isAuthor() || app.isCommentEditingAllowed());
 	}
 
 	// check if this is "our" autosaved comment
diff --git a/browser/src/docstatefunctions.js b/browser/src/docstatefunctions.js
index ccaa23b..4ec5615 100644
--- a/browser/src/docstatefunctions.js
+++ b/browser/src/docstatefunctions.js
@@ -131,8 +131,13 @@ app.setCursorVisibility = function (visible) {
 	app.file.textCursor.visible = visible;
 };
 
+app.isPdfDocument = function () {
+	var fileName = app.map && app.map['wopi'] ? app.map['wopi'].BaseFileName : '';
+	return typeof fileName === 'string' && fileName.toLowerCase().endsWith('.pdf');
+};
+
 app.isCommentEditingAllowed = function () {
-	return app.file.editComment;
+	return app.file.editComment && !app.isPdfDocument();
 };
 
 app.isRedlineManagementAllowed = function () {
@@ -153,8 +158,8 @@ app.setPermission = function (permission) {
 };
 
 app.setCommentEditingPermission = function (isAllowed) {
-	app.file.editComment = isAllowed;
+	app.file.editComment = isAllowed && !app.isPdfDocument();
 };
 
 app.setRedlineManagementAllowed = function (isAllowed) {
 	app.file.allowManageRedlines = isAllowed;
-- 
KOffice patch series